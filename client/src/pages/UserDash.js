import React from 'react';

const UserDash = () => {
  // notifications, set notifications w usestate?
  // need a mongoose message model? (or notif mode?)
  // use query to grab data from server
  // usere effect to watch that^
  return <div>UserDash</div>;
};

export default UserDash;
